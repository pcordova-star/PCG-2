// This file is intentionally left blank to resolve a build conflict between
// the App Router and the Pages Router. The root layout is now handled by src/app/layout.tsx.
// A default export that is a valid React component is still required to prevent build errors.

export default function Document() {
  return null;
}
