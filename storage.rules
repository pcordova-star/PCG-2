rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read, write and delete access for all users
    match /{allPaths=**} {
      allow read, write, delete;
    }
  }
}
